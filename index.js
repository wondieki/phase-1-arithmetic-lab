// Variables for multiplication test
const num1 = 31; // You can adjust these values as needed
const num2 = 2;
const multiply = num1 * num2; // This should equal 62

// Random integer greater than 0
const random = Math.floor(Math.random() * 100) + 1; // Generates a random integer between 1 and 100

// Variables for modulus test
const num3 = 10;
const num4 = 6;
const mod = num3 % num4; // This should equal 4

// Maximum value in a set
const values = [10, 15, 20, 5, 2];
const max = Math.max(...values); // This should return 20
